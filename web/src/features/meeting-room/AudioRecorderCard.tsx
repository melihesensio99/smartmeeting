import { Alert, Button, Card, CardContent, Stack, Typography } from '@mui/material'
import { useAudioRecorder } from '../../hooks/useAudioRecorder'
export function AudioRecorderCard() { const recorder = useAudioRecorder(); return <Card sx={{ height: '100%' }}><CardContent><Stack spacing={2}><Typography variant="h6">Canlı toplantı odası</Typography><Typography color="text.secondary">Ses kaydı Web Audio API arkasındaki hook ile yönetilir.</Typography><Alert severity={recorder.state === 'recording' ? 'warning' : 'info'}>{recorder.state === 'recording' ? 'Kayıt devam ediyor' : 'Kayıt başlatmaya hazır'}</Alert>{recorder.state === 'recording' ? <Button variant="contained" color="secondary" onClick={recorder.stop}>Kaydı durdur</Button> : <Button variant="contained" onClick={() => void recorder.start()}>Kaydı başlat</Button>}</Stack></CardContent></Card> }
