import { Alert, AppBar, Box, Card, CardContent, Chip, Container, Dialog, DialogContent, DialogTitle, Grid, IconButton, Stack, Toolbar, Typography } from '@mui/material'
import { useState } from 'react'
import { MeetingForm } from './features/calendar/MeetingForm'
import { useMeetings } from './features/calendar/useMeetings'
import { AudioRecorderCard } from './features/meeting-room/AudioRecorderCard'
import type { CreateMeetingInput } from './types/meeting'
import { useMeetingStatus } from './hooks/useMeetingStatus'
export function App() { useMeetingStatus(); const { meetings, create } = useMeetings(); const [open, setOpen] = useState(false); const submit = (input: CreateMeetingInput) => create.mutate(input, { onSuccess: () => setOpen(false) }); return <Box sx={{ minHeight: '100vh' }}><AppBar position="static"><Toolbar><Typography variant="h6" sx={{ flexGrow: 1 }}>SmartMeeting</Typography><Typography variant="body2">Akıllı Toplantı Asistanı</Typography></Toolbar></AppBar><Container maxWidth="lg" sx={{ py: 5 }}><Stack spacing={4}><Stack direction="row" sx={{ justifyContent: 'space-between', alignItems: 'center' }}><Box><Typography variant="h4">Toplantı merkezi</Typography><Typography color="text.secondary">Planla, kaydet ve yapay zekâ destekli çıktılara dönüştür.</Typography></Box><IconButton color="primary" onClick={() => setOpen(true)} sx={{ bgcolor: 'secondary.main', fontSize: 24 }}>+</IconButton></Stack>{meetings.isError && <Alert severity="error">Toplantılar yüklenemedi. API adresini ve backend’i kontrol edin.</Alert>}<Grid container spacing={3}><Grid size={{ xs: 12, md: 5 }}><AudioRecorderCard /></Grid><Grid size={{ xs: 12, md: 7 }}><Stack spacing={2}>{meetings.isLoading ? <Typography>Toplantılar yükleniyor…</Typography> : meetings.data?.length ? meetings.data.map((meeting) => <Card key={meeting.id}><CardContent><Stack direction="row" sx={{ justifyContent: 'space-between' }}><Box><Typography variant="h6">{meeting.title}</Typography><Typography color="text.secondary">{new Date(meeting.startsAt).toLocaleString('tr-TR')}</Typography></Box><Chip label={String(meeting.status)} color="primary" variant="outlined" /></Stack>{meeting.summary && <Typography sx={{ mt: 2 }}>{meeting.summary.overview}</Typography>}</CardContent></Card>) : <Card><CardContent><Typography>Henüz toplantı yok. İlk toplantınızı planlayın.</Typography></CardContent></Card>}</Stack></Grid></Grid></Stack></Container><Dialog open={open} onClose={() => setOpen(false)} fullWidth maxWidth="sm"><DialogTitle>Yeni toplantı</DialogTitle><DialogContent sx={{ pt: 2 }}><MeetingForm onSubmit={submit} loading={create.isPending} /></DialogContent></Dialog></Box> }
